{% extends 'base.html.twig' %}

{% block title %}Nouvelle entrée de stock{% endblock %}

{% block body %}
<div class="container-fluid px-3 px-md-4">
    <div class="row">
        <div class="col-12 col-lg-8 mx-auto">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1><i class="bi bi-plus-circle"></i> Nouvelle entrée de stock</h1>
                <a href="{{ path('app_stock_item_index') }}" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left"></i> Retour
                </a>
            </div>

            <div class="card border-0 shadow-sm">
                <div class="card-body">
                    {{ include('stock_item/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
