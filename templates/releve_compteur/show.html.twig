{% extends 'base.html.twig' %}

{% block title %}ReleveCompteur{% endblock %}

{% block body %}
    <h1>ReleveCompteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ releve_compteur.id }}</td>
            </tr>
            <tr>
                <th>DateReleve</th>
                <td>{{ releve_compteur.dateReleve ? releve_compteur.dateReleve|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CompteurNoir</th>
                <td>{{ releve_compteur.compteurNoir }}</td>
            </tr>
            <tr>
                <th>CompteurCouleur</th>
                <td>{{ releve_compteur.compteurCouleur }}</td>
            </tr>
            <tr>
                <th>Source</th>
                <td>{{ releve_compteur.source }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_releve_compteur_index') }}">back to list</a>

    <a href="{{ path('app_releve_compteur_edit', {'id': releve_compteur.id}) }}">edit</a>

    {{ include('releve_compteur/_delete_form.html.twig') }}
{% endblock %}
