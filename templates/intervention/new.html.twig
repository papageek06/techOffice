{% extends 'base.html.twig' %}

{% block title %}Nouvelle intervention{% if site %} - {{ site.nomSite }}{% endif %}{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-4">
        <h1 class="h2 mb-0">
            <i class="bi bi-tools"></i> Nouvelle intervention
            {% if site is defined and site %}
                <small class="text-muted fs-5">— Site {{ site.nomSite }}</small>
            {% endif %}
        </h1>
        <div class="d-flex gap-2">
            {% if site is defined and site %}
                <a href="{{ path('app_site_show', {'id': site.id}) }}" class="btn btn-outline-secondary">
                    <i class="bi bi-building"></i> Retour au site
                </a>
            {% endif %}
            <a href="{{ path('app_intervention_index') }}" class="btn btn-outline-secondary">
                <i class="bi bi-list"></i> Liste des interventions
            </a>
        </div>
    </div>

    <div class="alert alert-info small">
        <i class="bi bi-info-circle"></i>
        L'utilisateur de création (vous ou le site) ne peut pas être modifié. La date d'intervention sera renseignée automatiquement à la clôture (date de création). Seul le type d'intervention est obligatoire.
    </div>

    {{ include('intervention/_form.html.twig') }}
{% endblock %}
