<form method="post" action="{{ path('app_piece_delete', {'id': piece.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette pièce ?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ piece.id) }}">
    <button type="submit" class="btn btn-outline-danger w-100">
        <i class="bi bi-trash"></i> Supprimer
    </button>
</form>
