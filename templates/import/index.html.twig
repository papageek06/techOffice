{% extends 'base.html.twig' %}

{% block title %}Import CSV - TechOffice{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-12">
            <h1 class="mb-4">
                <i class="bi bi-upload"></i> Import CSV
            </h1>

            {% if error %}
                <div class="alert alert-danger d-flex align-items-center flex-wrap gap-2" role="alert">
                    <span class="flex-grow-1"><i class="bi bi-exclamation-triangle me-2"></i>{{ error }}</span>
                    <a href="{{ path('app_home') }}" class="btn btn-outline-danger btn-sm">
                        <i class="bi bi-arrow-left"></i> Retour à l'accueil
                    </a>
                </div>
            {% endif %}

            {% if result %}
                <div class="alert alert-{{ result.errors|length > 0 ? 'warning' : 'success' }}" role="alert">
                    <h4 class="alert-heading">
                        <i class="bi bi-{{ result.errors|length > 0 ? 'exclamation-triangle' : 'check-circle' }}"></i>
                        Import terminé
                    </h4>
                    <hr>
                    <p class="mb-2">
                        <strong>{{ result.success }}</strong> ligne(s) importée(s) avec succès
                    </p>
                    <p class="mb-2">
                        <strong>{{ result.skipped }}</strong> ligne(s) ignorée(s) (totaux ou données invalides)
                    </p>
                    {% if result.errors|length > 0 %}
                        <hr>
                        <p class="mb-0">
                            <strong>{{ result.errors|length }}</strong> erreur(s) :
                        </p>
                        <ul class="mb-0">
                            {% for error in result.errors %}
                                <li>{{ error }}</li>
                            {% endfor %}
                        </ul>
                    {% endif %}
                </div>
            {% endif %}

            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">
                        <i class="bi bi-file-earmark-spreadsheet"></i> Télécharger un fichier CSV
                    </h5>
                </div>
                <div class="card-body">
                    <form method="post" enctype="multipart/form-data">
                        <div class="mb-3">
                            <label for="csv_file" class="form-label">Fichier CSV</label>
                            <input type="file" class="form-control" id="csv_file" name="csv_file" accept=".csv" required>
                            <div class="form-text">
                                Format attendu : CSV avec colonnes CUSTOMER, SERIAL_NUMBER, MODEL, BRAND, READING_DATE, etc.
                            </div>
                        </div>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-upload"></i> Importer
                        </button>
                        <a href="{{ path('app_home') }}" class="btn btn-secondary">
                            <i class="bi bi-arrow-left"></i> Retour
                        </a>
                    </form>
                </div>
            </div>

            <div class="card mt-4 shadow-sm">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0">
                        <i class="bi bi-info-circle"></i> Instructions
                    </h5>
                </div>
                <div class="card-body">
                    <h6>Format du fichier CSV :</h6>
                    <ul>
                        <li>Le fichier doit contenir les colonnes : <code>CUSTOMER</code>, <code>SERIAL_NUMBER</code>, <code>MODEL</code>, <code>BRAND</code>, <code>READING_DATE</code></li>
                        <li>Les lignes de totaux (sans SERIAL_NUMBER) seront automatiquement ignorées</li>
                        <li>Les clients seront créés automatiquement s'ils n'existent pas</li>
                        <li>Les imprimantes seront identifiées par leur adresse IP ou numéro de série</li>
                        <li>Les relevés existants pour la même date ne seront pas dupliqués</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
